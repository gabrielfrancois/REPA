name: SiT
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python = 3.11
  - pytorch >= 1.13
  - torchvision
  - pytorch-cuda =12.1
  - pip
  - pip:
    - timm
    - diffusers
    - accelerate
    - torchdiffeq
    - wandb
    - peft
    - pillow
    - tqdm
    - datasets
